# JOURNAL-IA — Mini-TP 5

- L'IA a fait une revue de AppNavigation() complete. Elle a propose plusieurs remarques.

- La remarque la plus pertinente : elle a dit qu'il faudrait gerer le cas ou le produit n'est pas trouve avec un id invalide — par exemple si quelqu'un modifie l'URL. C'est vrai parce que si l'id n'existe pas, le ecran affiche rien et c'est confusing pour l'utilisateur.

- La remarque moins pertinente ICI : l'IA a suggere d'ajouter des routes typées et un ViewModel. C'est hors périmètre du jour, on a pas encore vu les routes typées ni le ViewModel (ça vient en séance 6 et 7). Pour le moment la version simple avec string route et find fonctionne bien.
